* repainting the pie leaves label artifacts

* size & shape

    * resizing card-text font

    * shifting the gauge down

    * make size of charts responsive to window size

* ordering a JSON high to low (so slicing actually makes sense)

*  is 'bubble' the same thing as 'scatter'